'use client'
import React from 'react'

const AddToCart = () => {
    return (
        <>
            <button className='button' onClick={() => alert('Button Clicked')}>
                Click
            </button>
        </>
    )
}

export default AddToCart
